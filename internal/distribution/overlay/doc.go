// Package overlay owns the Config Server's read-side overlay evaluator. Its
// inputs are Server read-model values; it never depends on the Admin product's
// release or overlay write model.
package overlay
